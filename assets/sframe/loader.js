class Loader{
    
    constructor(startFile, local){
        this.startFile = startFile;

        if (window.File && window.FileReader && window.FileList && window.Blob) {
        } else {
        alert('Este não suporta requisitos necessários para a execução deste WebAPP.');
        }
    }

    function start(){
        this.arquivoInicial

        
        
    }

    function reader(){
        reader
    }

}